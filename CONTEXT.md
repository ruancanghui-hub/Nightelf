# Nightelf

本地优先的 macOS AI 资源工作台。Vault 里的资源是普通文件；应用负责整理、编辑与打开，默认不执行它们。

## Language

**Vault**：
用户选定的本地资源库根目录。资源以普通文件和文件夹存放，可被 Finder、编辑器和 Git 直接使用。
_Avoid_: 仓库, 工作区根, 库

**启动器**：
Vault 侧栏中的一种资源，保存标题和一条脚本路径，用来调起项目里已有的一键启动脚本。它不是脚本文件本身，也不把脚本复制进 Vault。
_Avoid_: 脚本栏目, 运行脚本, 脚本文件

**脚本路径**：
启动器配置的 `.sh` 或 `.command` 文件的本机绝对路径。脚本仍留在原项目目录。
_Avoid_: 收集路径, 启动命令
